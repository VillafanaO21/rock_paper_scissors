extens layout

block content
    div.card-container
        div.card Player Choice
            img(src=`/images/${playerChoice}-icon.jpg`)

        p vs
        div.card Pc Choice
            img(src=`/images/${pcChoice}-icon.jpg`)


            h3 Winner: #{winner}

            a(href=`/`) Play Again